# 형변환

# 변수를 문자와 정수로 초기화
var1 = '123'
var2 = 123

# 문자와 정수를 사용해서 덧셈 연산을 시도하면 예외가 발생한다
# print(var1+var2)

# 문자(열)를 정수로 변경
# 문자(열)를 정수로 변환하면 정수의 덧셈 연산이 가능하다.
print(int(var1)+var2)

# 정수를 문자(열)로 변경
# 정수를 문자(열)로 변환하면 문자의 덧셈 연산이 가능하다.
print(var1+str(var2))

# 'hello'는 정수로 형변환될수없다.
# print(int('hello'))